import React from 'react';
import GameCard from './GameCard';

// Setup and loop over intinal grid - 20 cards - 2 sets of 10 matching pais 

export default function GameGrid() {
  return (
      <div className="game-grid">
        <GameCard />
      </div>
  );
}